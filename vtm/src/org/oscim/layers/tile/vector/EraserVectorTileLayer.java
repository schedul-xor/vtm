package org.oscim.layers.tile.vector;

import org.oscim.map.Map;
import org.oscim.tiling.TileSource;

public class EraserVectorTileLayer extends VectorTileLayer {
    public EraserVectorTileLayer(Map map, TileSource tileSource) {
        super(map, tileSource);
    }
}
